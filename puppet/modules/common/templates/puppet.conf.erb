[main]
  pluginsync = true

[master]
  reports = store, http
  node_terminus = exec
  external_nodes = /usr/bin/env PUPPET_DASHBOARD_URL=http://localhost:3000 /opt/dashboard/bin/external_node
  allow_duplicate_certs = True
  node_name = facter

[agent]
  report = true
  runinterval = 300
